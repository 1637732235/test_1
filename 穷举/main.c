//#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main()
{
	int a, b, d=0;
	for (a = 1; a <= 3; a++)
	{
		for(b = 1; b <= 1; b++)
		{
			//for(c =1; c <= 4; c++)
			
				if (1 * a + 2 * b  == 4 && a + b  == 3);
				{
					d++;
				}	
		}
		printf("%d\n", d);
			return 0;		
	}
}